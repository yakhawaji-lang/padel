diff --git a/node_modules/@capacitor-community/contacts/ios/Plugin/ContactsPlugin.swift b/node_modules/@capacitor-community/contacts/ios/Plugin/ContactsPlugin.swift
index f9214f1..28caac2 100644
--- a/node_modules/@capacitor-community/contacts/ios/Plugin/ContactsPlugin.swift
+++ b/node_modules/@capacitor-community/contacts/ios/Plugin/ContactsPlugin.swift
@@ -202,8 +202,9 @@ public class ContactsPlugin: CAPPlugin, CNContactPickerDelegate {
     }
 
     public func contactPicker(_ picker: CNContactPickerViewController, didSelect selectedContact: CNContact) {
-        let call = self.bridge?.savedCall(withID: self.pickContactCallbackId ?? "")
-
+        let callbackId = self.pickContactCallbackId
+        self.pickContactCallbackId = nil
+        let call = self.bridge?.savedCall(withID: callbackId ?? "")
         guard let call = call else {
             return
         }
@@ -218,4 +219,14 @@ public class ContactsPlugin: CAPPlugin, CNContactPickerDelegate {
 
         self.bridge?.releaseCall(call)
     }
+
+    /// iOS: required so the Capacitor JS promise settles when the user closes the picker without choosing a contact.
+    public func contactPickerDidCancel(_ picker: CNContactPickerViewController) {
+        picker.dismiss(animated: true, completion: nil)
+        guard let callbackId = pickContactCallbackId else { return }
+        pickContactCallbackId = nil
+        guard let call = self.bridge?.savedCall(withID: callbackId) else { return }
+        call.reject("User cancelled.", "CANCELLED")
+        self.bridge?.releaseCall(call)
+    }
 }
