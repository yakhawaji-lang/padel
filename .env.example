# PostgreSQL — قاعدة البيانات (انظر POSTGRES_SETUP.md و VERCEL_DEPLOY.md)
# الحفظ إلى PostgreSQL افتراضياً. لاستخدام المتصفح: VITE_USE_POSTGRES=false

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/padel

# مزامنة ثنائية الاتجاه:
#   npm run sync-to-cloud   — من المحلي للسحابة
#   npm run sync-from-cloud — من السحابة للمحلي
# DATABASE_URL_CLOUD=postgresql://user:pass@host/db?sslmode=require

# Supabase — مزامنة البيانات بين الأجهزة (عند عدم استخدام PostgreSQL)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_public_key_here

# WhatsApp Business API — استقبال الرسائل والإشعارات
WHATSAPP_VERIFY_TOKEN=playtix_whatsapp_verify
# WHATSAPP_ACCESS_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
